server {
    listen 80;
    server_name _;

    location / {
        return 200 '{"message": "Hello from upstream!", "server": "nginx"}';
        add_header Content-Type application/json;
    }

    location /health {
        return 200 '{"status": "ok"}';
        add_header Content-Type application/json;
    }
}
